{
  "name": "scorecard-app",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean-slate": "npx lerna clean",
    "setup-project": "npm i && npx lerna bootstrap --hoist",
    "start-web": "lerna run --scope @dm/web-vite dev --stream",
    "start-storybook": "lerna run --scope @dm/web-vite storybook --stream",
    "start-server": "echo \"look ma, I started the server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "danmorales209@gmail.com",
  "license": "ISC",
  "dependencies": {
    "lerna": "^5.1.8"
  }
}
